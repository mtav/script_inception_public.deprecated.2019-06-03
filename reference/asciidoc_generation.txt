# the old way:
asciidoc -a toc README.txt

# the new way:
asciidoc ./README.asciidoc

# then to update gh-pages
git commit -am "asciidoc upgrade"
git pull && git push
git checkout gh-pages
git checkout master README.html
mv README.html index.html
git rm README.html
git commit -am "asciidoc upgrade"
git pull && git push
git checkout master

# asciidoc icon location:
$ cat ~/.asciidoc/asciidoc.conf
[attributes]
iconsdir=/.../.asciidoc/images/icons/
